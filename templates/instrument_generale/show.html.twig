{% extends 'base.html.twig' %}

{% block title %}InstrumentGenerale{% endblock %}

{% block body %}
    <h1>InstrumentGenerale</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ instrument_generale.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ instrument_generale.designation }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ instrument_generale.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('instrument_generale_index') }}">back to list</a>

    <a href="{{ path('instrument_generale_edit', {'id': instrument_generale.id}) }}">edit</a>

    {{ include('instrument_generale/_delete_form.html.twig') }}
{% endblock %}
